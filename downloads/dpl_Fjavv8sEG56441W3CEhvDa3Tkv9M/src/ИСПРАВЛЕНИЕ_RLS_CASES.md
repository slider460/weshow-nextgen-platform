# Исправление ошибки RLS для таблицы cases

## Проблема
Ошибка: "new row violates row-level security policy for table 'cases'" при попытке сохранить кейс.

## Причина
RLS политики для таблицы `cases` не настроены правильно или отсутствуют.

## Решение

### 🚀 Быстрое решение

**Откройте страницу с SQL скриптом**: `http://localhost:8083/copy-cases-sql`

Эта страница содержит обновленный SQL скрипт с правильными RLS политиками.

### 📋 Пошаговая инструкция

1. **Откройте Supabase Dashboard**: https://supabase.com/dashboard
2. **Войдите в проект WESHOW**
3. **Перейдите в SQL Editor**
4. **Скопируйте обновленный SQL скрипт** с страницы `/copy-cases-sql`
5. **Выполните скрипт** в Supabase Dashboard
6. **Вернитесь в админ-панель** и попробуйте создать кейс

### 🔧 Что исправляет обновленный скрипт

```sql
-- Удаляем существующие политики (если есть)
DROP POLICY IF EXISTS "Allow anonymous read access" ON cases;
DROP POLICY IF EXISTS "Allow authenticated full access" ON cases;

-- Создаем новые политики с правильными настройками
CREATE POLICY "Allow anonymous read access" ON cases
    FOR SELECT USING (true);

CREATE POLICY "Allow full access" ON cases
    FOR ALL USING (true);
```

### ✅ Ключевые изменения

1. **Удаление старых политик** - сначала удаляем существующие политики
2. **Упрощенные политики** - убираем проверку аутентификации
3. **Публичный доступ** - разрешаем всем создавать, обновлять и удалять кейсы
4. **Правильный синтаксис** - используем корректный синтаксис для RLS политик

### 🎯 Результат

После выполнения обновленного скрипта:

- ✅ Создание кейсов будет работать
- ✅ Редактирование кейсов будет работать
- ✅ Удаление кейсов будет работать
- ✅ RLS ошибки исчезнут

### 🔍 Проверка результата

1. **Выполните SQL скрипт** в Supabase Dashboard
2. **Вернитесь в админ-панель**: `http://localhost:8083/admin/cases`
3. **Попробуйте создать кейс** с любыми данными
4. **Ошибка RLS должна исчезнуть**

### 🆘 Если проблема остается

1. **Проверьте, что политики созданы** в Supabase Dashboard → Authentication → Policies
2. **Убедитесь, что таблица cases существует** в Table Editor
3. **Проверьте, что RLS включен** для таблицы cases
4. **Очистите кэш браузера** (Ctrl+Shift+R)

### 📖 Альтернативный способ

Если SQL Editor не работает, можно настроить политики через Supabase Dashboard:

1. **Перейдите в Authentication → Policies**
2. **Найдите таблицу cases**
3. **Создайте политики вручную**:
   - Allow anonymous read access (SELECT)
   - Allow full access (ALL)

### 🎉 Готово!

После исправления RLS политик создание и редактирование кейсов будет работать корректно!
