name: 🔍 Check Secrets

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🔍 Check all secrets
        run: |
          echo "=== Проверка секретов ==="
          
          # Проверяем VERCEL_TOKEN
          if [ -n "${{ secrets.VERCEL_TOKEN }}" ]; then
            echo "✅ VERCEL_TOKEN: Установлен (длина: ${#VERCEL_TOKEN})"
          else
            echo "❌ VERCEL_TOKEN: НЕ УСТАНОВЛЕН"
          fi
          
          # Проверяем VERCEL_ORG_ID
          if [ -n "${{ secrets.VERCEL_ORG_ID }}" ]; then
            echo "✅ VERCEL_ORG_ID: Установлен (длина: ${#VERCEL_ORG_ID})"
          else
            echo "❌ VERCEL_ORG_ID: НЕ УСТАНОВЛЕН"
          fi
          
          # Проверяем VERCEL_PROJECT_ID
          if [ -n "${{ secrets.VERCEL_PROJECT_ID }}" ]; then
            echo "✅ VERCEL_PROJECT_ID: Установлен (длина: ${#VERCEL_PROJECT_ID})"
          else
            echo "❌ VERCEL_PROJECT_ID: НЕ УСТАНОВЛЕН"
          fi
          
          echo ""
          echo "=== Информация о проекте ==="
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"